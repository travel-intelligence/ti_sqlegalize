# TiSqlegalize

**TiSqlegalize** is a Rails Engine serving a [JSON API](http://jsonapi.org/) representation of a relational data domain.

The media type supporting this web service, known as [RelJSON](http://github.com/ebastien/reljson) is still at an experimental stage.
